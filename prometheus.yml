global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:

  # Scrape metrics from OTEL Collector (converted OTLP → Prometheus format)
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8889"]
